import { Database } from "$entities/example/example";

const Database: Database = {
	find: () => {
		return;
	},
	create: () => {
		return;
	},
	delete: () => {
		return;
	},
};

export default Database;
